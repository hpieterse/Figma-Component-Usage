import { ComponentUsageNamePipe } from './component-usage-name.pipe';

describe('ComponentUsageNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ComponentUsageNamePipe();
    expect(pipe).toBeTruthy();
  });
});
